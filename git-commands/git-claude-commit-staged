#!/bin/bash

# Source Claude helper functions
source ~/code/dotfiles/claude-functions.bash

if ! git diff --cached --quiet; then
    run_claude "create a git commit for the currently staged changes" "text" "Bash"
else
    echo "No staged changes to commit"
fi
