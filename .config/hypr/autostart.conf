# Extra autostart processes
# exec-once = uwsm app -- my-service

# Start hypridle for idle management and auto-sleep
exec-once = hypridle

# Automatically manage stack workspace visibility and restore state
exec = ~/.config/hypr/scripts/stack_workspace_manager.sh

# Start Waybar (handled by omarchy default config)
