[user]
	email = surya.gaddipati@gmail.com
	name = Surya G
[credential "https://github.com"]
	helper =
	helper = !/usr/bin/gh auth git-credential
[credential "https://gist.github.com"]
	helper =
	helper = !/usr/bin/gh auth git-credential
[pull]
	# For a cleaner, more linear history, prefer rebasing over creating merge commits.
	rebase = true
[push]
	# `default = current` is deprecated. `autoSetupRemote = true` is the modern
	# equivalent of `push -u` on the first push to a new branch.
	autoSetupRemote = true
[alias]
	s = status
	push = push -u origin HEAD
	p = push -u origin HEAD
	co = checkout
	b = branch
	wtd = worktree remove
[init]
	defaultBranch = main
[core]
	editor = vim
[includeIf "gitdir:~/code/hudl/"]
	path = ~/code/hudl/.gitconfig-hudl
