#!/usr/bin/env bash
#MISE description="Complete installation: dependencies + dotfiles + dev tools"
#MISE depends=["deps:install-packages", "deps:install", "install", "deps:install-dev", "deps:install-lsp"]

printf "\n"
printf "${GREEN}🎉 Full installation complete!${NC}\n"
printf "${YELLOW}Next steps:${NC}\n"
printf "  1. Restart your terminal or run: source ~/.bashrc\n"
printf "  2. Start tmux: tmux or t\n"
printf "  3. Open neovim: nvim (plugins will install automatically)\n"
printf "  4. Install MCP servers: mise run install-mcp\n"